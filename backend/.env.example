# 로컬 개발 전용 예시 설정이다. app/app 계정은 내 PC의 개발용 Docker DB에서만 사용하며,
# 운영·공용 서버에는 절대 사용하지 않는다. 사용 시 backend/.env로 복사한다.
APP_ENV=development
DATABASE_URL=postgresql+psycopg://app:app@localhost:5432/sy_holdings_dev
